# Bundled micro-lessons

These short, silent caption videos are generated by `scripts/generate_course_videos.py` and are bundled with the Android demo so playback never opens a browser or depends on a third-party video host.

Each MP4 is mapped to one course lesson in `src/data/courses.ts`. The visible title and teaching points in every video match that lesson's article summary.
